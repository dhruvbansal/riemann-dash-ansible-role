input {
  file {
    path => "{{ riemann_dash_log_dir }}/*.log"
    type => "riemann"
  }
{% if ansible_os_family == "Debian" %}  
  file {
    path => "/var/log/upstart/riemann-dash.log"
    type => "riemann"
  }
{% endif %}
}